# data/

Working directory for downloaded videos, extracted audio, and sampled
frames. Created automatically by `src.config.ensure_dirs()` on first run.

Contents here are git-ignored (see `.gitignore`) — this folder should stay
empty in version control aside from this file.
